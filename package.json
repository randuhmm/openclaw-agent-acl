{
  "name": "@randuhmm/openclaw-agent-acl",
  "version": "1.0.2",
  "description": "Per-agent MCP tool access control for OpenClaw",
  "type": "module",
  "scripts": {
    "build": "tsup index.ts --format esm --outDir dist --external openclaw --external node:fs --external node:path --external node:http --external node:url --publicDir ui",
    "plugin:build": "npm run build",
    "plugin:validate": "clawhub package validate .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ],
    "compat": {
      "pluginApi": ">=2026.3.24-beta.2",
      "minGatewayVersion": "2026.3.24-beta.2"
    },
    "build": {
      "openclawVersion": "2026.6.10"
    }
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "@vitest/coverage-v8": "^2.0.0",
    "tsup": "^8.5.1",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=22.19"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/randuhmm/openclaw-agent-acl"
  },
  "license": "MIT",
  "keywords": [
    "openclaw",
    "openclaw-plugin",
    "mcp",
    "acl",
    "agent",
    "tool-policy"
  ],
  "peerDependencies": {
    "openclaw": "^2026.6.8"
  }
}

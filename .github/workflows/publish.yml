name: Publish

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  id-token: write  # required for ClawHub OIDC trusted publishing
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          registry-url: 'https://registry.npmjs.org'

      - run: npm ci

      - name: Install clawhub CLI
        run: npm install -g clawhub

      - name: Build plugin
        run: npm run build

      - name: Validate plugin package
        run: clawhub package validate .

      - name: Publish to ClawHub
        run: clawhub package publish randuhmm/openclaw-agent-acl
        env:
          # Not required for workflow_dispatch after trusted-publisher is configured
          CLAWHUB_TOKEN: ${{ secrets.CLAWHUB_TOKEN }}

      - name: Publish to npm
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
